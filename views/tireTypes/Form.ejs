<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <link rel="icon" href="/img/favicon-96x96.png" type="image/png">
  <script src="https://kit.fontawesome.com/939dfff437.js" crossorigin="anonymous"></script>
  <title><%= tireType ? 'Editar Tipo de Vehículo' : 'Registrar Tipo de Vehículo' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/navButtons.css">
</head>
<body>

  <%- include('../partials/navbar') %>

  <div class="action-buttons" style="display: flex; flex-wrap: wrap; gap: 12px; margin-bottom: 20px; margin-top: 20px; margin-left: 5px;">
    <a href="/tiretypes/list" class="btn-action">
      📋 Ir a la lista de tipos de vehículo
    </a>
    <a href="/users/dashboard" class="btn-action">
      🏠 Ir al Dashboard
    </a>
  </div>

  <div class="container">
    <h1 class="titulo"><%= tireType ? 'EDITAR TIPO DE VEHÍCULO' : 'REGISTRO DE TIPO DE VEHÍCULO' %></h1>
    
    <form action="<%= action %>" method="POST">
      
      <div class="form-group">
        <label for="name">Nombre del tipo de vehículo</label>
        <input type="text" id="name" name="name" placeholder="Ej: Auto, Camión, SUV" value="<%= tireType?.name || '' %>" required>
      </div>

      <div class="form-actions">
        <button type="submit">
          <%= tireType ? 'ACTUALIZAR' : 'REGISTRAR' %>
        </button>
      </div>
    </form>
  </div>
</body>
</html>